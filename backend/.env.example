DATABASE_URL=postgresql://crypto_agent:dev_password@localhost:5432/crypto_news
OPENROUTER_API_KEY=sk-or-v1-...
OPENAI_API_KEY=sk-...
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Debug Mode - Set to 'true' or '1' to enable comprehensive step-by-step logging
# When enabled, every operation logs STARTED and FINISHED phases with detailed context
# This helps track application flow and identify silent errors or missing stages
DEBUG=false

# LangFuse - LLM Observability (https://langfuse.com)
# Get your keys from: https://cloud.langfuse.com -> Settings -> API Keys
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
# Optional: Use custom host for self-hosted LangFuse (defaults to https://cloud.langfuse.com)
# LANGFUSE_BASE_URL=https://cloud.langfuse.com

# Security - Optional API Key Authentication
# If set, all /ask and /api/job-status requests require X-API-Key header
# Leave unset for public access (rate limiting still applies)
# API_KEY=your-secret-api-key-here
